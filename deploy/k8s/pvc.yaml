apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gobivc-data
  namespace: gobivc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # storageClassName: standard  # uncomment and set to your cloud's default storage class
